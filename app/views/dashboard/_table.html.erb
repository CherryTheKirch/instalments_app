<table class="table">
  <thead>
  <tr>
    <th scope="col">#</th>
    <th scope="col">Nazwa</th>
    <th scope="col">Kwota</th>
    <th scope="col">Termin</th>
    <th scope="col">Pozostało</th>
  </tr>
  </thead>
  <tbody>
    <% @user.instalments.each do |instalment, index|%>
    <tr>
      <th scope="row"><%= index %></th>
      <td><%= instalment.title %></td>
      <td><%= instalment.amount %></td>
      <td><%= instalment.deadline %></td>
      <td><%= days_left(deadline) %></td>
    </tr>
    <% end %>
  </tbody>
</table>
